import { TextInput, Text, TouchableOpacityy } from "react-native"
import { Picker } from '@react-native-picker/picker';

export default function Schedule({oneHorario, Room1, setRoom1, Room2, setRoom2}) {
    
}(){
  
}
